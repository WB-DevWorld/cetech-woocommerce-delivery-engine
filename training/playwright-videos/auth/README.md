# Auth for video capture

This folder intentionally has **no** committed storage state.

Video scenarios reuse:

`training/playwright/auth/storage-state.json`

Create it with:

```bash
npm run training:auth
```

Never commit that file.
