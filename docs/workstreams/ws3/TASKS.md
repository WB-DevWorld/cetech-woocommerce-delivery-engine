# WS3 Tasks — wbdevworld

## WS3-P0 — Canonical organization repository and source recovery
- organization transfer completed while preserving published RC.9 lineage;
- recover/classify local-only branches/tags/stashes/untracked/evidence;
- prove integrated.2 source/docs SHAs;
- preserve unique WPML stream;
- publish only refs intentionally approved for collaboration;
- reconcile authority/status docs.

## WS3-P1 — Control plane / CI
- integrate AGENTS/ownership/current-work/authority/workstream files after comparing existing repo docs;
- implement plugin-specific CI from existing Composer/PHPUnit/npm/package tooling;
- establish PR/checkpoint/review conventions;
- set up repository protections to the extent GitHub plan/tooling permits.

## WS3-Q1 — Cache/session/security qualification
Prove two-session isolation with WP Rocket + Redis where legitimately reproducible; recheck final combined WCFM/admin/privacy boundaries. Do not reopen unrelated exploratory security work without evidence.

## WS3-I1 — Neutral RC.10 qualification integration
Create/manage the neutral `batch/**` candidate, import exact owner SHAs, run combined gates, and produce one compatibility matrix.
