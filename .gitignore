/vendor/
composer.lock

# Packaging / build artifacts
/dist/
/build/
*.zip

# Tests / coverage
tests/.phpunit.cache/
.phpunit.result.cache
coverage/

# Local / secrets
*.log
.env
.env.local
docs/woodmart/
.DS_Store
Thumbs.db
desktop.ini

# Local read-only FLAIROC verification scratch (may contain page HTML dumps)
/tmp-stage5b2-verify/
/tmp-read-only-compare/

# Stage 3 disposable dry-run WordPress stubs (generated at runtime)
scripts/stage3-wp-stubs/
